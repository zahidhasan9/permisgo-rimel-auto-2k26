export const metadata = {
    title: "Disclaimer",
    description: "Driving School",
};

export default function RootLayout({ children }) {
    return (
        <>
            {children}
        </>
    )
}