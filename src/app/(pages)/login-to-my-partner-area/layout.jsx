export const metadata = {
    title: "Log in to my partner area",
    description: "Driving School",
};

export default function RootLayout({ children }) {
    return (
        <>
            {children}
        </>
    )
}