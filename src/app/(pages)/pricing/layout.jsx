export const metadata = {
    title: "Pricing",
    description: "Driving School",
};

export default function RootLayout({ children }) {
    return (
        <>
            {children}
        </>
    )
}