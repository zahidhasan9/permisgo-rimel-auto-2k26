export const metadata = {
    title: "Monitor Privacy Policy",
    description: "Driving School",
};

export default function RootLayout({ children }) {
    return (
        <>
            {children}
        </>
    )
}